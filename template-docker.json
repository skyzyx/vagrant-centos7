{
  "builders": [
    {
      "type": "docker",
      "image": "centos:7.3.1611",
      "export_path": "builds/centos7-x64-docker.tar"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "execute_command": "echo 'vagrant' | sh '{{.Path}}'",
      "override": {
        "docker": {
          "scripts": [
            "scripts/base.sh",
            "scripts/configs.sh",
            "scripts/packages.sh",
            "scripts/cleanup.sh"
          ]
        }
      }
    }
  ]
}
